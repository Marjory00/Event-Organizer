
/**
 * @file Vendor model
 * @description Defines the data structure for a vendor.
 */
export interface Vendor {
  id: number;
  name: string;
  service: string;
  contactPerson: string;
  phoneNumber: string;
  email: string;
}
